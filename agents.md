# PokeFlow - 2D 픽셀 게임기 포켓덱스

## 🌟 프로젝트 개요
PokeFlow는 포켓몬 API(PokeAPI)를 활용해 구축한 인터랙티브 웹 애플리케이션입니다. 
**[중요 테마 업데이트]**: 이 프로젝트의 디자인 언어는 3D 스큐어모피즘에서 **2D 플랫 픽셀 아트(Flat Pixel Art & Neo-Brutalism)**로 전환되었습니다. 기기 자체가 평면적인 고전 게임 그래픽처럼 보여야 합니다.

## 🚀 핵심 기능
1. **2D 플랫 UI**: 굵은 검은색 테두리(`border: 4px solid #000`), 그라데이션 없는 순수 단색, 블러(Blur) 없는 딱딱한 그림자.
2. **단일 뷰(Single View)**: 한 번에 한 마리의 포켓몬만 스크린에 표시하며 십자키로 탐색합니다.
3. **픽셀 아트 중심**: 스크린 내부와 렌즈 하이라이트 등 모든 묘사는 도트를 찍은 것처럼 평면적이어야 하며 `image-rendering: pixelated;`를 사용합니다.

## 🤖 AI 에이전트 지시사항 & 디자인 철학 (필수 준수 사항)
- **플랫(Flat) 렌더링 강제**: CSS 그라데이션(`linear-gradient`, `radial-gradient`), 투명도, 반사광, `box-shadow: inset`을 **절대 사용하지 마세요.**
- **만화적 외곽선**: 기기 본체, 버튼, 렌즈 등 눈에 띄는 모든 객체는 굵은 검은색 테두리를 가져야 합니다.
- **물리적 타격감**: 버튼(`:active`) 클릭 시 부드러운 전환 없이 즉각적으로 요소가 대각선 아래로 이동(`transform`)하고 그림자가 사라져야 합니다.
